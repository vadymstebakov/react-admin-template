import * as registerTypes from './types';
import * as registerActions from './actions';
import registerReducer from './reducer';

export { registerTypes, registerActions };
export default registerReducer;
